import 'package:flutter/material.dart';

class AppColors {
  // Colores principales - Universidad Continental
  static const Color primary = Color(0xFF003DA5); // Azul UC
  static const Color primaryDark = Color(0xFF002870);
  static const Color primaryLight = Color(0xFF3D6FBF);

  static const Color secondary = Color(0xFFFF6B00); // Naranja UC
  static const Color secondaryDark = Color(0xFFCC5500);
  static const Color secondaryLight = Color(0xFFFF8C33);

  // Colores de fondo
  static const Color background = Color(0xFFF5F7FA);
  static const Color cardBackground = Colors.white;

  // Colores de texto
  static const Color textPrimary = Color(0xFF1A1A1A);
  static const Color textSecondary = Color(0xFF6B7280);
  static const Color textHint = Color(0xFF9CA3AF);

  // Colores de estado
  static const Color success = Color(0xFF10B981);
  static const Color error = Color(0xFFEF4444);
  static const Color warning = Color(0xFFF59E0B);
  static const Color info = Color(0xFF3B82F6);

  // Colores adicionales
  static const Color divider = Color(0xFFE5E7EB);
  static const Color shadow = Color(0x1A000000);
}
